# @generated by Hollywood. Do not edit by hand.

name: CI
on:
  push:
    branches:
      - main
  pull_request: {}
  workflow_dispatch: {}
permissions: {}
concurrency:
  group: ${{ format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true
jobs:
  rust:
    name: Rust
    runs-on: macos-15
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Format
        run: cargo fmt --check
        shell: bash
      - name: Clippy
        run: cargo clippy --workspace -- -D warnings
        shell: bash
      - name: Test
        run: cargo test --workspace
        shell: bash
      - name: Doc
        run: cargo doc --workspace --no-deps
        env:
          RUSTDOCFLAGS: -D warnings
        shell: bash
      - name: Build release
        run: cargo build --release
        shell: bash
      - name: Check file size
        uses: ./.github/actions/check-file-size
        with:
          file: target/release/microvm
          max-bytes: "1048576"
  workflows:
    name: Generated workflows
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - name: Install dependencies
        run: npm ci
        shell: bash
      - name: Regenerate workflows
        run: npm run generate
        shell: bash
      - name: Regenerate actions
        run: npm run actions
        shell: bash
      - name: Require clean generated output
        run: git diff --exit-code -- .github/actions .github/workflows
        shell: bash
      - uses: rhysd/actionlint@a443f344ff32813837fa49f7aa6cbc478d770e62
  ci:
    name: ci-allgreen
    if: ${{ always() }}
    needs:
      - rust
      - workflows
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Accept complete CI
        if: ${{ needs.rust.result == 'success' && needs.workflows.result == 'success' }}
        run: "true"
        shell: bash
      - name: Reject incomplete CI
        if: ${{ !(needs.rust.result == 'success' && needs.workflows.result == 'success') }}
        run: "false"
        shell: bash
