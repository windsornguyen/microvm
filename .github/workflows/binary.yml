# @generated by Hollywood. Do not edit by hand.

name: Binary
on:
  release:
    types:
      - published
permissions:
  contents: write
jobs:
  build:
    name: Build
    runs-on: macos-15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
        shell: bash
      - name: Sign
        run: codesign --sign - --entitlements entitlements.plist --force target/release/microvm
        shell: bash
      - name: Prepare package
        run: mkdir -p dist
        shell: bash
      - name: Copy binary
        run: cp target/release/microvm dist/
        shell: bash
      - name: Copy entitlements
        run: cp entitlements.plist dist/
        shell: bash
      - name: Archive
        run: tar -C dist -czf ../microvm-darwin-arm64.tar.gz .
        shell: bash
      - name: Checksum archive
        uses: ./.github/actions/checksum-archive
        with:
          archive: microvm-darwin-arm64.tar.gz
      - name: Upload
        uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda
        with:
          files: |-
            microvm-darwin-arm64.tar.gz
            microvm-darwin-arm64.tar.gz.sha256
